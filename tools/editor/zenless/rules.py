"""绝区零（zenless-zone-zero）专属规则常量。

与原神/星铁的差异集中在这里，解析逻辑见 parse.py、推理见 inference.py。
所有规则都用真实公告核对过，核对样本见 fixtures/（由 selftest.py 断言）。

绝区零的信息结构最省事：每版本开服当天发的**停服更新公告**是一份总纲，版本名、
本版起止、「下个版本日期」（写作「X.Y版本结束时间为 …」）全在里面。这是三条线里
唯一一个版本边界由公告**明写**、不用任何外推的游戏——原神靠 42 天周期锚点，星铁
靠相邻更新日相减，绝区零直接抄。总纲的其余两个用处：高难的**期数**、以及所有
用「X.Y版本更新后 / X.Y版本结束」相对写法的活动日期。
"""

# ─── 抓取参数 ────────────────────────────────────────────

GAME = "zenless-zone-zero"
BILI_UID = 1636034895        # 绝区零官方账号

# 与原神/星铁同宽：总纲是每版本的「总目录」，必须留在抓取窗口内。实测 40 条
# 覆盖约 56 天，窗口里同时留着 3.1、3.2 两份总纲（相对日期要按版本号查表，
# 见 parse.resolve_period，旧的也得在）。
PAGE_SIZE = 40
DYN_LIMIT = 80

# ─── 公告分类 ────────────────────────────────────────────

# 停服更新公告 = 总纲（subject 形如「3.2版本「她与她的隐秘往事」更新公告」）
VERSION_NOTES_PATTERN = r"\d+\.\d+版本「[^」]+」更新公告"
# 预下载通知（T−2）。它给的日期与总纲的【更新开始时间】一致，不另解析：
# 版本更新日只有一个权威来源，两个来源能对上的信息不值得多一条解析路径。
PRELOAD_KEYWORD = "预下载"
# 卡池公告（subject 形如「3.2版本限时频段（上期）」；3.1 首期没有「（上期）」后缀）
BANNER_KEYWORD = "限时频段"
# 大月卡
BATTLE_PASS_KEYWORD = "丽都城募"

# 活动公告的固定后缀。绝区零的活动公告一律是「「活动名」活动说明」，用它当判据
# 比维护一份排除词名单可靠——名单会过期，而公告后缀是个格式约定。
# 实测被它挡掉的（都不以「活动说明」结尾）：3.2版本「商城」上新说明、
# 新剧情：主线第三季第三章「…」、《云·绝区零》3.2版本更新说明、
# 3.2版本已知问题及游戏优化说明、「迷宫诡域」常驻玩法开启。
ACTIVITY_SUFFIX = "活动说明"

# 常驻玩法开启不录入（已定，2026-09-21）：只值得记一次，且不在总纲的「全新活动」
# 段里、拿不到日期。上面那条后缀判据已经能挡掉现有写法，这里再挡一次词面，
# 免得将来换个写法（如「X」常驻玩法开启活动说明）悄悄混进来。
EXCLUDE = ["常驻玩法"]

# ─── 正文段落 ────────────────────────────────────────────

# 绝区零用【】标段名（原神是〓、星铁是▌）
ACTIVITY_SECTION = "【活动时间】"

# 版本大活动的判据：正文有这两个段标记之一。措辞不固定（用户实测两种都在用），
# 但都是「限时结束后收录进常驻」这个意思，与时长无关——恰浪花逐夏而至（3.1）
# 只有 40 天也是版本大活动，而占满整版的「法厄同年度大揭秘」是网页活动。
PERMANENT_MARKERS = ("【活动常驻说明】", "【丽都纪事】")

# 登录福利的判据：正文出现这些词之一，都是「累计天数领奖」型。
# 与原神既有类型同名，不自造新类型。
LOGIN_KEYWORDS = ("累计登录", "登录即可", "邀请好友")

# 正文之外的外链（非米游社图床）即网页活动。实测「法厄同年度大揭秘」正文里的
# 活动入口是 >>>点击前往参与活动<<< 指向 https://mhyurl.cn/…，
# 而其余活动的 content 里只有 upload-bbs.miyoushe.com 的配图。
IMAGE_HOST = "upload-bbs.miyoushe.com"

# 结束时刻在清晨（≤ 06:00）视为「该游戏日尚未开始」，结束日减一天；
# 白天的时段（11:59 / 14:59）照抄。**起点不减**：04:00 开的双倍掉落当天就能打。
# 同一套口径，核对依据见 selftest.py。
MORNING_CUTOFF_MIN = 6 * 60

# ─── 高难 ────────────────────────────────────────────────

# 总纲的 ▶ 段只列敌人，不给日期，只给「第一期/第二期/第三期」。网格见 inference.py
ENDGAME_MODES = ("危局强袭战", "式舆防卫战")
ENDGAME_SECTION = r"▶\s*(危局强袭战|式舆防卫战)(.*?)(?=▶|【|$)"
# 期数从正文数出来（3.2 的危局写成「第一期试炼 / 第一期绝境」，所以要取去重后的期号）
ENDGAME_INDEX = r"第([一二三四五六七八九十]+)期"

# ─── 标题格式 ────────────────────────────────────────────

GAME_TITLE = "绝区零"
# 与数据文件既有条目一致：《绝区零》3.2 版本「她与她的隐秘往事」停服更新
VERSION_UPDATE_TITLE = f"《{GAME_TITLE}》{{ver}} 版本「{{name}}」停服更新"
LIVESTREAM_TITLE = f"《{GAME_TITLE}》{{ver}} 版本「{{name}}」前瞻特别节目"
# 丽都城募的期名每版本都一样，必须带版本号才能与上一期区分（否则去重键会撞）
BATTLE_PASS_TITLE = "{ver}版本「丽都城募」"
# 卡池标题沿用数据文件既有的手工格式：调频活动-<限定S级代理人，按正文出现顺序>
BANNER_TITLE_PREFIX = "调频活动-"
# 高难标题自带 MMDD（危局强袭战0911），与数据文件一致
ENDGAME_TITLE = "{mode}{mmdd}"

# ─── 颜色 ────────────────────────────────────────────────
# 取值与数据文件里既有的条目一致（高难、版本更新、前瞻、版本大活动都是手工定的，
# 与封面取色无关，所以固定下来；常规活动/卡池/网页活动走封面取色）
COLORS = {
    "危局强袭战": "#f5a3a3",
    "式舆防卫战": "#a0b9f8",
    "版本更新": "#ffffff",
    "前瞻直播": "#cdede6",
    "版本大活动": "#f7dbdb",
    "大月卡": "#d9c9a8",      # 与星铁的「无名勋礼」同色，同类条目同色
}
# 常规活动 / 卡池 / 网页活动走封面图取色（复用 common.colors.pastel_from_url）
FALLBACK_COLOR = "#cce0f0"
