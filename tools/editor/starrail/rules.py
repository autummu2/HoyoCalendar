"""星铁（崩坏：星穹铁道）专属规则常量。

与原神的差异集中在这里，解析逻辑见 parse.py。所有规则都用真实公告核对过，
核对样本见 fixtures/（由 selftest.py 断言）。
"""

from common.keys import TAG_PENDING_VERSION  # noqa: F401  （再导出，值只在 keys 定义一处）

# ─── 抓取参数 ────────────────────────────────────────────

GAME = "honkai-star-rail"
BILI_UID = 1340190821        # 崩坏：星穹铁道官方账号

# 比原神的 30 宽：版本更新说明是每版本的「总纲」（下版本日期 + 高难期名日期都在里面），
# 必须保证它留在窗口内。实测 4.4 的说明已排到第 44 条（page_size=30 会漏掉）。
PAGE_SIZE = 40
DYN_LIMIT = 80

# 星铁**没有**周期兜底天数：版本周期不固定（4.4→4.5 = 42 天，4.5→4.6 = 33 天），
# 起止只能由相邻两次更新日相减得出。拿不到就等下一次公告，不外推（见 PLAN.md §5）。

# ─── 公告分类 ────────────────────────────────────────────

# 版本更新说明的小标题（subject 形如「4.5版本「挥掷千星的筹码」版本更新说明」）
VERSION_NOTES_KEYWORD = "版本更新说明"
# 更新预告（subject 形如「4.5版本预下载&更新预告」），版本更新日最权威的来源
UPDATE_PREVIEW_KEYWORD = "更新预告"
# 卡池公告（subject 形如「4.5版本活动跃迁（其一）」）
BANNER_PATTERN = r"\d+\.\d+版本活动跃迁"
# 大月卡
BATTLE_PASS_KEYWORD = "无名勋礼"

# 非活动公告的排除词。命中即不当作「常规/版本大活动」——它们分别由
# parse_version_notes / parse_update_preview / parse_banner_body 单独处理，
# 或本就不该进日历（反馈帖、封禁公示、云游戏推广等）。
EXCLUDE = [
    "版本更新说明", "更新预告", "预下载", "活动跃迁", "无名勋礼",
    "开拓任务", "开拓续闻", "账号封禁", "商店上新", "意见反馈",
    "优化说明", "已知问题", "跃迁记录", "小程序", "云•星穹铁道",
    "角色前瞻", "光锥前瞻", "更新电台", "专题展示页", "互动抽奖",
    "联名", "创作者激励",
    # 以下三条是 2026-09-21 实测补的：它们的标题带「」，会被当成活动，但正文里
    # 根本没有活动时间段，于是每轮都白抓一次正文、又永远落不了盘（无日期被闸掉）。
    "任务说明",    # 开拓任务的公告（「鸣笛于归寂之时」任务说明）——「开拓任务」这个词未必在标题里
    "赛季扩充",    # 常驻玩法的版本内更新（「货币战争•零和博弈」赛季扩充说明 V4.4）
    "权益升级",    # 《云•星穹铁道》的促销，崩铁侧也会发一条同名公告
    # 2026-09-24 实测补：同一个常驻玩法的更新说明，这期标题改叫「更新说明」
    # （「货币战争•零和博弈」更新说明 V4.6），「赛季扩充」就漏掉了，于是每轮白读一次
    # 正文、又永远解不出活动段。它同时覆盖上面那条「版本更新说明」——但版本更新/预告/
    # 卡池公告的安全不靠这里：is_activity 先判 is_version_notes / is_update_preview /
    # is_banner，而收集它们的 find_* 根本不看 EXCLUDE，所以这里加词不会误杀它们。
    "更新说明",
]

# ─── 正文段落 ────────────────────────────────────────────

# 星铁的段落标记是「▌」而非原神的「〓」。时间段的段名有三种，实测都在用：
#   超限：狂飙大奖赛 → ▌限时活动期   镇伏贪饕 → ▌活动时间   无名勋礼 → ▌开启时间
# 「■」也收：版更公告里同一个时间段可能标成 ■活动时间（「星际幻宠」乐园就是），
# 而 ■ 本来就是它的小节标记。段名集合保持很小，所以不会误取到 ■开放时间/■抽取概率。
ACTIVITY_SECTION = r"[▌■]\s*(活动时间|限时活动期|开启时间)"

# 版本更新说明里高难玩法的段落（期名与日期都在这里）
GAMEPLAY_SECTION = r"■玩法(.*?)(?=■|▌|$)"

# 高难玩法分四支，型名与数据文件里的一致
ENDGAME_MODES = ("异相仲裁", "末日幻影", "虚构叙事", "混沌回忆")

# 登录福利的判据：总纲的全新活动段里**没有 `活动时间：`** 的那一条（实测只有「巡星之礼」，
# 每版都有），说明写的是「登录就领签到奖励」这类措辞。与原神/绝区零既有类型同名，不自造。
# 与绝区零 LOGIN_KEYWORDS 同理：判据是本文，不是名字——名单会过期，还会把将来的同类漏掉。
LOGIN_KEYWORDS = ("每日登录", "登录即可", "累计登录", "签到")

# 结束时刻在清晨（≤ 06:00）视为「该游戏日尚未开始」，结束日减一天；
# 白天时段（11:59 / 15:00）照抄。核对依据见 selftest.py。
MORNING_CUTOFF_MIN = 6 * 60

# ─── 标题格式 ────────────────────────────────────────────

GAME_TITLE = "崩坏：星穹铁道"
VERSION_UPDATE_TITLE = f"《{GAME_TITLE}》{{ver}} 版本「{{name}}」停服更新"
# 只知道日期、还不知道版本名时的占位标题（如版本更新说明刚给出下版本日期，
# 而 B站前瞻还没开）。等更新预告/B站上线动态来了由 calibrate 补全。
VERSION_PLACEHOLDER_TITLE = f"《{GAME_TITLE}》{{ver}} 版本停服更新"
LIVESTREAM_TITLE = f"《{GAME_TITLE}》{{ver}} 版本「{{name}}」前瞻特别节目"
# 无名勋礼的期名固定不变，必须带版本号才能与上一期区分（否则去重键会撞，只落一期）
BATTLE_PASS_TITLE = "{ver}版本「无名勋礼」"

# ─── 颜色 ────────────────────────────────────────────────
# 高难四支各一色，取值与数据文件里既有的条目一致
COLORS = {
    "异相仲裁": "#f7d9a2",
    "末日幻影": "#abbfed",
    "虚构叙事": "#faea9e",
    "混沌回忆": "#cff0a8",
    "版本更新": "#ffffff",
    "前瞻直播": "#a9ceed",
    "大月卡": "#d9c9a8",
    "版本大活动": "#f0d9b0",
}
# 常规活动与卡池走封面图取色（复用 common.colors.pastel_from_url）
FALLBACK_COLOR = "#cce0f0"

# ─── 推理标签 ────────────────────────────────────────────

# TAG_PENDING_VERSION（「版本号与日期均为预测，公告后回填」）在文件顶部从 keys 再导出：
# 它由 calibrate 在校准到权威来源时摘掉，产出侧与清除侧必须是同一个字符串。
