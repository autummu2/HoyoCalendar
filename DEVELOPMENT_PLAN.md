# 🎮 米游活动日历 (HoyoCalendar) — 开发计划书

> **版本**: v1.0.0  
> **创建日期**: 2026-07-28  
> **最后更新**: 2026-09-25  
> **状态**: ✅ 全部 Sprint 完成；原神（6.3）、星铁（6.4）、绝区零（6.5）、未定（6.6）四条自动化维护管线均已落地。
> 各管线的方案 / 现状 / 待办见 `tools/editor/<游戏>/PLAN.md`——**交接先读那四份文档的 §0「交接速览」**  
> **许可证**: GPL v3.0
> **产品经理**: AI辅助设计 + 人工确认

---

## 目录

1. [项目愿景与目标](#1-项目愿景与目标)
2. [目标用户画像](#2-目标用户画像)
3. [核心功能规划](#3-核心功能规划)
4. [MVP 范围定义](#4-mvp-范围定义)
5. [技术架构方案](#5-技术架构方案)
6. [数据来源与处理策略](#6-数据来源与处理策略)
7. [UI/UX 设计方向](#7-uiux-设计方向)
8. [项目里程碑与迭代计划](#8-项目里程碑与迭代计划)
9. [Git 分支策略与协作规范](#9-git-分支策略与协作规范)
10. [GitHub 项目管理配置](#10-github-项目管理配置)
11. [风险与待确认项](#11-风险与待确认项)

---

## 1. 项目愿景与目标

### 1.1 愿景

**成为米哈游玩家一站式活动信息查看工具**——让玩家不再遗漏任何活动奖励，不再在各个游戏公告之间来回切换。

### 1.2 核心价值主张

| 痛点 | 解决方案 |
|------|----------|
| 多个米游的活动公告分散在各个平台（游戏内、微博、B站、米游社） | 统一聚合展示，一个页面看全所有游戏活动 |
| 活动时间重叠时容易遗漏 | 时间线可视化 + 提醒功能（远期） |
| 无法快速判断"今天有什么能做的" | 按日期筛选，当日活动高亮 |
| 新玩家不了解过往活动节奏 | 历史活动归档查询 |

### 1.3 成功指标

- MVP阶段：能够手动录入并展示至少3款游戏的活动数据
- 用户体验：用户打开页面后3秒内能看到当日活动
- 数据覆盖：覆盖原神、崩坏：星穹铁道、绝区零、未定事件簿

---

## 2. 目标用户画像

| 画像 | 特征 | 核心需求 |
|------|------|----------|
| **多修玩家** 👥 | 同时玩2+款米游 | 一眼看清所有游戏活动重叠情况，合理分配时间 |
| **单修肝帝** 🎯 | 深度玩1款游戏 | 关注活动的精确起止时间，不错过限时奖励 |
| **回归玩家** 🔄 | 退坑后回归 | 快速了解当前版本有哪些活动在进行中 |
| **休闲玩家** ☕ | 随缘上线 | 知道大版本更新时间，决定什么时候回坑 |

---

## 3. 核心功能规划

### 3.1 功能全景图

```
HoyoCalendar
├── 📅 日历视图 (核心)
│   ├── 月/周/日视图切换
│   ├── 活动连续色条（Paimon.moe 风格）
│   ├── 游戏三态筛选（包含/排除/无）
│   ├── 活动类型三态筛选
│   ├── 活动完成状态筛选
│   └── 点击色条/日期 → 详情面板
│
├── 🎨 活动卡片
│   ├── 活动详情侧栏（EventDetail）
│   ├── 日视图卡片列表（DayView）
│   ├── 完成状态标记切换
│   └── 🔗 活动链接跳转
│
├── 🛠️ 数据编辑器（Python 桌面应用）
│   ├── ✏️ tkinter GUI 表单编辑
│   ├── 📥 公告解析器（米游社 API + 规则提取）
│   ├── 🏷️ 动态类型/标签管理
│   ├── 📅 日期选择器 + 🎨 选色器
│   ├── 🔍 活动列表筛选排序
│   └── 🚀 一键推送 GitHub + Vercel 部署
│
└── ⚙️ 设置
    └── 主题切换（亮色/暗色/跟随系统）
```

### 3.2 支持的游戏列表

| 游戏 | 英文名 | 发行年份 | 备注 |
|------|--------|----------|------|
| 原神 | Genshin Impact | 2020 | 用户量最大 |
| 崩坏：星穹铁道 | Honkai: Star Rail | 2023 | 用户量第二 |
| 绝区零 | Zenless Zone Zero | 2024 | 最新游戏，活跃用户多 |
| 未定事件簿 | Tears of Themis | 2020 | 乙女向，用户群不同；2026-09-25 接入（见 6.6） |

### 3.3 活动分类体系

```
活动类型（可自定义增删）
├── 🃏 卡池        — 角色/武器/音擎 UP
├── 📋 常规活动    — 签到/登录/福利/小游戏
├── 🎭 版本大活动  — 版本主题活动/庆典/海灯节
├── 🔄 版本更新    — 停服维护/版本上线
├── 🌐 网页活动    — 联动/线下/网页活动
└── 🏆 高难挑战    — 深渊/混沌回忆/式舆防卫战/危战
```

---

## 4. MVP 范围定义

### 4.1 MVP (v0.1.0) — 静态日历展示

**目标**: 最快速度交付一个可用的日历页面，手动录入数据。

**包含**:
- ✅ Web单页应用 (React)
- ✅ 月视图日历组件
- ✅ 支持3款游戏：原神、星穹铁道、绝区零
- ✅ 每活动独立配色（color 字段）
- ✅ 动态活动类型（可增删改）
- ✅ 点击活动查看详情（名称、时间、游戏、类型、描述）
- ✅ 今日高亮 & 当前活动标记
- ✅ 数据存储在 JSON/YAML 文件中（无需后端）
- ✅ 响应式布局（桌面端为主，移动端可查看）
- ✅ 暗色/亮色主题

**不包含**:
- ❌ 用户系统
- ❌ 自动数据抓取
- ❌ 推送通知
- ❌ 国际化

### 4.2 v1.0.0 — 完整功能

- 月/周/日三视图
- 游戏+类型三态筛选（包含/排除/无）
- 活动完成状态标记 + localStorage 持久化
- 色条点击查看详情 + 高亮联动
- 浅色配色主题 + 暗色/亮色切换
- 今日蓝色实心圆高亮
- 活动链接可点击跳转
- Vercel 自动部署
- GitHub Actions CI/CD
- Python 可视化编辑器（tkinter GUI）
- 公告解析器（米游社 API + 规则提取）
- 一键推送更新到 GitHub + 自动部署
- GPL v3.0 开源许可证

### 4.3 远期规划

- 定时自动抓取公告 + 自动生成数据 PR
- 多数据源（HoYoLAB、B站）支持
- 活动推送通知
- 国际化（多语言）

---

## 5. 技术架构方案

### 5.1 MVP 技术栈（推荐方案）

```
┌──────────────────────────────────────────────────┐
│                    前端 (Frontend)                 │
│  React 19 + TypeScript + Vite                     │
│  ├── UI:  Tailwind CSS 4                          │
│  ├── 日历: 自定义组件 (基于 Css Grid)             │
│  ├── 状态:  React Context + useReducer            │
│  └── 部署:  Vercel / GitHub Pages                 │
├──────────────────────────────────────────────────┤
│                    数据层 (Data)                   │
│  ├── 活动数据:  YAML/JSON 静态文件                │
│  ├── 数据校验:  Zod schema                        │
│  └── 图片资源:  CDN / 本地 static                 │
├──────────────────────────────────────────────────┤
│                  工具链 (Tooling)                  │
│  ├── 包管理:   npm                                │
│  ├── 代码规范: ESLint + Prettier                  │
│  ├── 测试:     Vitest + React Testing Library     │
│  ├── CI/CD:    GitHub Actions                     │
│  └── 文档:     Storybook (组件文档)               │
└──────────────────────────────────────────────────┘
```

### 5.2 远期架构演进 (v1.0+)

```
┌──────────┐    ┌──────────────┐    ┌──────────┐
│  Frontend │───▶│  API Gateway │───▶│  Backend  │
│  (React)  │    │  (Koa/FastAPI)│    │  Services │
└──────────┘    └──────────────┘    └──────────┘
                                          │
                      ┌───────────────────┼───────────────┐
                      ▼                   ▼               ▼
                ┌──────────┐      ┌──────────┐    ┌──────────┐
                │ PostgreSQL│      │  Redis   │    │  Crawler │
                │ (活动数据) │      │ (缓存)    │    │ (抓取)   │
                └──────────┘      └──────────┘    └──────────┘
```

### 5.3 备选技术栈方案

| 方案 | 适用场景 | 优缺点 |
|------|----------|--------|
| **Flutter/Dart** | 移动端优先 | 跨平台一套代码，但Web性能不如原生React |
| **Vue 3 + Nuxt** | SSR需求 | 生态完善，但社区组件库不如React丰富（日历类） |
| **Next.js** | SSR + API Routes | 全栈能力，但MVP阶段过重 |
| **纯HTML+JS** | 极简MVP | 最快原型，但扩展性差 |

---

## 6. 数据来源与处理策略

### 6.1 阶段策略

```
Phase 1 (MVP)      Phase 2 (半自动)      Phase 3 (自动化)
─────────────────  ────────────────────  ─────────────────
手动录入 YAML      脚本辅助抓取          全自动采集+AI解析
人工校对           人工审核确认          人工抽查+异常告警
Git管理数据版本    数据录入后台          CMS系统
少量活动样本        完整活动覆盖          实时更新
```

### 6.2 MVP 数据模型 (YAML)

```yaml
# data/events/genshin-impact.yaml
- id: "gi-2026-08-version"
  game: "genshin-impact"
  title: "「韶光抚月」版本活动"
  type: "版本大活动"          # 活动类型（卡池/常规活动/版本大活动/版本更新/网页活动/高难挑战）
  description: "参与活动获取限定武器「贯虹之槊」与大量原石"
  color: "#DBEAFE"           # 可选：色条背景色（#RRGGBB），不指定则用游戏品牌色
  bar_bg_image: ""           # 可选：色条背景图片URL（优先级高于 color）
  start_date: "2026-08-15"
  end_date: "2026-09-26"
  banner_image: "/images/events/gi-shaoguang.jpg"
  phases:                    # 子阶段（可选）
    - title: "第一章"
      start_date: "2026-08-15"
      end_date: "2026-08-28"
    - title: "第二章"
      start_date: "2026-08-29"
      end_date: "2026-09-12"
  tags: ["版本活动", "限定武器"]
  source_url: "https://ys.mihoyo.com/..."
```

### 6.3 原神自动化维护管线（已实现）

工具位于 `tools/editor/genshin/`（共用底层在 `tools/editor/common/`），
规则细节见 `tools/editor/genshin/RULES.md`，现状与待办见 `tools/editor/genshin/PLAN.md`。

```
米游社公告栏 ──┐
              ├─→ extractor 分类 + 正文解析 ─┐
B站官号动态 ──┘                              ├─→ 合并/推理 ─→ extracted_full.json
                                             │                      │
米游社维护预告 ─→ 版本更新日期/版本名 ────────┘                      ↓
                                                          apply_events 只新增
                                                          calibrate   只修正已确认
                                                                    ↓
                                                     data/events/genshin-impact.yaml
```

**两条互不干涉的职责，是整套自动化鲁棒性的基础：**

| | 新增（`apply_events.py`） | 校准 / 修正维护（`calibrate.py`） |
|---|---|---|
| 做什么 | 只新增，**绝不改动已有条目** | 只改已有条目的值，**绝不新增** |
| 判定 | 主键（`keys.event_key`）不在数据里 | 主键在数据里，且**未打 `calibrated` 标记** |
| 依据 | 米游社公告 + B站动态 + 版本节奏推理 | B站总览 / B站活动动态 / B站前瞻公告 / 米游社维护预告 |
| 时机 | 每次运行 | 每次运行，但每个条目只处理一次 |

**关键设计决策（2026-09-21）：**

1. **主键只表示「身份」，与日期无关**。日期是**值**，会被校准修改；主键是**身份**，一旦确定不再变。
   早期用 `(标题, 开始日期)` 当去重键，导致「日期一改旧条目就认不出来 → 重复插入」。
   2026-09-21 起活动类**有意**破例（决策 8）：标题会跨版本重名，不带日期就会静默丢数据，
   代价用「同名 7 天兜底 + 排除日期会被校准改写的类型」封住。
2. **`id` 永不改变**：前端用 `id` 做 React key 与「已完成」追踪键，改了会丢用户的完成标记。
3. **一次性标记 `calibrated`**（值为确认依据）。有标记不再重复处理；本轮找不到依据就不打标记，
   下次再试（避免「假确认」）。只处理最近 30 天内的条目——更久远的来源动态早已被 B站列表刷掉。
4. **版本锚点从数据文件推导**，不硬编码。版本延期 / 版本号跳号只需人工改数据文件里那一行，
   后续所有推理自动跟着走。这是「人工干预」的唯一入口，且只需一次。
5. **常规活动不纳入校准**：B站活动动态只覆盖多阶段的版本大活动，常规活动没有可靠的
   外部日期来源，纳进来只会产生假确认。
6. **不需要状态文件**：标记写在数据条目自身，无外部状态；新增只增不改、校准一次性，
   因此不存在与人工编辑的写冲突。
7. **已落盘条目不抓正文**（`_needs_body`）：正文只用于取日期/描述/配图，都是新增才需要的；
   预筛复用同一套去重（`keys.find_duplicate`），所以不会比 apply 的去重更激进。
   请求数因此从每次运行 ~20 降到稳态 0~2（2026-09-21 实测原神 1 / 星铁 2 / 绝区零 0），
   直接打在米游社风控的根因（请求数）上。
   命中的条目只跳过抓取、**不从列表移除**——校准的权威来源取自 `merged`/`banners`，剪掉会
   连带丢掉卡池/版本大活动的校准依据。
   唯一的一票否决是 `keys.PENDING_FIELD`：带该标记的条目（从绝区零总纲落盘、还缺公告才有的
   字段）**必须**重抓正文，预筛跳过它 = 那几样永远补不上。所以绝区零稳态请求数会随待补全
   条目数略升（见 6.5），换来的是活动能提前 12~35 天进日历。
8. **活动类按 `(标题, 开始日期)` 定身份**（2026-09-21 二次修订，`keys.ACTIVITY_TYPES`）。
   起因是最先只有周期性事件踩到的那个坑：标题重复时只认标题，会让新一期被当成重复条目
   **静默丢掉**（星铁「位面分裂」9 月那期实测：公告抓到了、日期也解析对了，就是没进日历；
   绝区零「全新放送」3.0 与 3.2 同名同类，是同一个坑）。
   原来只对**已知会重复**的名字开口子（`DATED_KEYED_PREFIXES` 名单），现在改成
   常规活动 / 登录福利 / 网页活动**一律**带开始日期——活动名没有规律
   （绝区零的两个福利槽每版本换名字，3.1 叫「云端礼赠」「『嗯呢』大派送！」、3.2 叫
   「全新放送」「『嗯呢』从天降」，四个名字互不相同也不复用），名单必然漏，而漏的代价是丢数据。
   这是第 1 条的**受控例外**，靠两条约束兜住：① `find_duplicate` 补「同名 7 天兜底」
   （与版本类同一套），日期被人工订正后仍能认回；② **日期会被校准改写的类型不能进来**
   ——版本大活动因此排除，`ACTIVITY_TYPES` 是从「公告可能落到的四种类型」里减掉它派生的。
   类型的同步副作用：类型不再进身份，于是「先按默认类型落盘、等公告到了再订正」不会插重复
   （见 6.5 绝区零那节）。周期性事件（`DATED_KEYED_PREFIXES`）维持原样。
9. **「待确认」标签的定义只放 `common/keys.py` 一处**，产出侧与清除侧都引用它（2026-09-21 修）。
   标签含义是「这个字段还没有权威来源确认过」——原神的版本大活动类型来自 B站多阶段动态、
   版本更新/前瞻的版本号是 +0.1 预测，产出时先打标；`calibrate` 拿到权威来源核对完
   **连同 `calibrated` 标记一起把它摘掉**。摘除点放在「确认」这一刻，产出侧摘掉等于没标过。
   此前两边各写一份字面量（产出侧写 `待确认`、清除侧只认 `待确认版本`），于是原神的类型标签
   **永远摘不掉**，在日历上成了永久徽章。定义收进 `keys.PENDING_TAGS` 后这类漂移不可能再发生。

### 6.4 星铁自动化维护管线（已实现并验证，2026-09-21；2026-09-22 扩充）

工具位于 `tools/editor/starrail/`，规则见 `starrail/RULES.md`，方案见 `starrail/PLAN.md`。

星铁与原神是**并行的两条线**，不是同一套逻辑的参数化：公告的段落标记、卡池标题来源、
版本节奏都不同。共用的只有 `common/` 下的底层模块（`yaml_io` / `keys` / `calibrate` /
`extractor` / `bilibili` / `apply_events` / `colors`）。

```
米游社公告栏 ──┐
              ├─→ parse 分类 + 解析 ─→ calibrate 校准 ─→ apply_events 只新增
B站官号动态 ──┘         │                                        ↓
                        └─→ starrail/extracted_hsr.json → data/events/honkai-star-rail.yaml
```

**星铁不需要周期性推理**（原神的 `inference.py` 在星铁完全用不上），因为米游社的
**版本更新说明**是一份每版本开服当天发布的总纲：

- `4.5版本的持续时间为 2026/08/26 4.5版本更新后 - 2026/09/28 06:00`
  → 一开服就知道**下个版本的准确日期**，不必按周期外推（星铁版本周期不固定：
  4.4→4.5 = 42 天，4.5→4.6 = 33 天，原神的「锚点 + 42×n」在这里是错的）
- `■玩法` 段 → **高难四支的期名与日期全在里面**（原神的高难无公告，只能推理）
- `6、全新活动` 段（段号每版不同，按后缀「全新活动」定位）→ **本版全部活动名与
  `活动时间：`**，比活动自己的公告早约 15 天（4.5 实测量）。这一段走**总纲优先**，
  与绝区零同一套机制（见 6.5）

总纲优先的落盘 / 订正流程、`pending` 标记与两条护栏见 6.5；星铁这边多一条自己的约束：
**只收当前版本的那份总纲**——窗口里旧版本的总纲，其活动要么已经落盘、要么永远不会落盘，
而从它产候选有实测风险（4.4 总纲写 `命运/银河铁道之夜`、公告与数据文件写 `「幻造：圣杯战争」`，
同一活动两个名字，按标题去重认不出来会插重复）。窗口里旧版本的说明**不再读**（2026-09-22 起：
版本周期表改由已落盘的版本更新条目推，旧说明没有用处了）。

2026-09-22 又补四样：**一帖多活动**（一篇公告挂多条活动，逐块解析）、**登录福利**
（`巡星之礼` 从总纲本文即可定类型与整版时段）、**正文缓存**（`common/body_cache.py`，
稳态正文请求降到 0）、**活动公告的审读闸门**（还有块的日期解不出来才再读，块全落盘就跳过）。
八类活动（版本更新 / 前瞻直播 / 卡池 / 高难四支 / 常规活动 / 版本大活动 / 大月卡 / 登录福利）
现已全部自动化，详见 `starrail/PLAN.md` §1。

其余差异（段落标记 `▌`/`■` vs `〓`、卡池 5 星名在正文里、无名勋礼期名固定、
嵌套书名号）见 `starrail/RULES.md` §五。

星铁的验证方式是**离线自检**（`starrail/selftest.py` + `fixtures/`，144 项全通过）——
`parse.py` 全是纯函数，规则改动不必反复联网。自检加强时抓到过一条静默漏数据
（双倍掉落类同名第二期被当成重复跳过），修法与复验见 `starrail/PLAN.md` §4，
机制见上面 6.3 的第 8 条。

### 6.5 绝区零自动化维护管线（已实现并实盘跑通，2026-09-21）

工具位于 `tools/editor/zenless/`，规则见 `zenless/RULES.md`，方案见 `zenless/PLAN.md`。
与 6.3/6.4/6.6 并列的一条线，共用 `common/` 下的底层模块。

```
米游社公告栏 ──┬─→ 停服更新公告（总纲）→ 版本更新日 / 版本终点 / 高难期数
              ├─→ 活动说明 / 限时频段 / 丽都城募 → 标题 + 类型 + 日期
B站官号动态 ──┴─→ 前瞻直播日期
                        ↓
        calibrate 校准 ─→ zenless/extracted_zzz.json → data/events/zenless-zone-zero.yaml
```

**绝区零的总纲给得最多**：停服更新公告直接写着版本更新日
（`【更新开始时间】2026/09/09 06:00`）和版本终点
（`3.2版本结束时间为 2026/10/21 06:00，该版本共持续42天`），所以**版本边界不用推**——
比星铁还省事，星铁至少还要自己减那个 06:00。四类实体的来源分工：

| 实体 | 来源 | 关键点 |
|---|---|---|
| 版本更新 | 总纲 `【更新开始时间】` | 版本终点 = 公告写的「版本结束时间」**减一天**（那是下个版本的更新日） |
| 高难挑战 | 总纲 `▶危局强袭战`/`▶式舆防卫战` 的 **期数** | 只给期数不给日期 → 按网格推（更新日后首个周五起，每周交替，每期 14 天） |
| 卡池 / 活动 / 大月卡 | 各自的公告 | 标题逐字取自公告 subject；类型与日期出自正文 |
| 前瞻直播 | B站官号动态 | 米游社不发前瞻预告（与原神同） |

三个判断都做成了**派生判据**而不是名单（见记忆 `no-hardcoding-prefer-derived-rules`）：
活动公告用 `活动说明` **后缀**筛（不是排除名单）；期数从正文**数** `第N期`（不写死 3）；
高难的那条已知例外（数据里的 `危局强袭战0729`）不需要例外名单——网格只按**当前版本**推，
旧版本早已不是当前版本，永远不会与它撞车。

**活动改为「总纲优先」**（2026-09-21，方案见 `zenless/PLAN.md` §1.2）。落地时的原方案就是
解析总纲的活动段，中途因为「总纲供不出活动**类型**，而那时类型是去重主键的一部分」改成
只取公告；决策 8 把类型移出主键后那条拦路的理由消失，再把总纲优先做回来。

两半现在都齐了：

1. **落盘**：总纲 `X、全新活动` 段逐个列出活动名与活动时间（`<p>` 段落，`text` 压平过
   解析不了），按默认类型 `常规活动` + `pending` 标记落盘。
2. **订正**：`calibrate.correct_from_candidates` 用本轮候选订正已有条目的类型。这是新开
   的通道而不是 `VALUE_FIELDS` 的扩展——它按**标题**匹配，因为要订正的恰恰包含类型本身。
   两条护栏：候选类型是默认值时不写回（它代表「没有肯定信号」）；候选自己带 `pending`
   时整条跳过（总纲那条每轮都会再出现，拿它「补全」只会白清标记，此后预筛按已录入跳过
   正文，描述与配图再也补不上）。

收益是**提前**：总纲比活动自己的公告早 12 / 12 / 35 天给出日期（三个版本实测），
所以整版活动能在版本更新当天挂上日历，而按公告落盘要等到公告发布（平均只比活动开始早 2 天）。
两个来源的日期经交叉核对**逐条一致**（21 个重叠活动），没有牺牲日期准确度。

**接线**（2026-09-21 追加，两半之外的第三件事）：两处只在**整条 `run()` 离线跑起来**才暴露的缺陷——
   ①产物字段白名单漏了 `pending`，标记写不进数据文件 → 下一轮预筛认不出待补全的条目 →
   整条路静默空转（活动照落盘，描述与配色永远补不上）；②取色排在订正之后，订正要补的
   `color` 来自候选、而候选的 `color` 正是取色填的 → 永远补不到（日志还写着「补齐描述与配色」）。
   两处都在绝区零与星铁各修一次（新增 `OUT_FIELDS` 常量 + 调整为
   日期闸 → 取色 → 订正 → 校准 → 落盘），并各加一条自检守卫断言白名单内容。
   教训：解析/订正函数的单测全绿也盖不住「接线错」——所以总纲优先的验收方式是**把整条
   `run()` 的联网部分打桩跑三轮**（落盘 → 订正 → 稳态零重复），细节见 `zenless/PLAN.md` §4
   里「2026-09-21 — 追加一轮」。

**注意**：共用层（订正通道 + `find_duplicate` 兜底放宽）已接进原神 / 星铁 / 绝区零三条管线（未定不接校准，见 6.6），**星铁也用上了同一套
总纲优先**（见 6.4）。绝区零的**总纲优先已实盘跑通**——数据文件 45 → 48 条，新增的正是
方案预测的那三条（「『嗯呢』从天降」「数据悬赏-实战模拟」「回归丽都 羽落重逢」）。

验证方式同星铁：**离线自检**（`zenless/selftest.py` + `fixtures/`，255 项全通过；
此前未通过的 7 项是 `zenless/PLAN.md` §6.2 的既有数据待订正项，2026-09-21 已随那 10 条
订正一并清零）。真实跑通一轮：正文抓取成功 16 / 失败 0 / 风控 0。

### 6.6 未定事件簿自动化维护管线（已实现并实盘跑通，2026-09-25）

工具位于 `tools/editor/themis/`，规则见 `themis/RULES.md`，方案见 `themis/PLAN.md`。
与 6.3/6.4/6.5 并列的一条线，共用 `common/` 下的底层模块，但**是四条里唯一「只增不改」的**：
未定不接 `common.calibrate`（没有校准机制），也没有总纲。

```
米游社资讯栏 ──→ 活动帖 → 标题 / 类型 / 日期 / 标签 / 配色 ─┐
米游社公告栏 ──→ 版本停服公告 → 版本号 + 开服日 ──────────┴─→ themis/extracted_tea.json
                                                            → data/events/tears-of-themis.yaml
```

**两个栏目都要抓**：活动在资讯栏（`type=3`），公告栏只有运维帖——但**版本停服更新公告在
公告栏**，版本更新这条只能从那里取（`extractor.fetch_post_list` 因此支持显式指定栏目）。
预筛四道，后两道落在正文上，不是关键词名单：

| 闸 | 判据 | 实测代价 |
|---|---|---|
| ⓪ 列表 | 帖子 id 已在数据文件里（只增不改 ⇒ 按 id 认） | 跳过 2 / 31，**不取正文** |
| ① 标题 | 负向名单（活动PV / 新卡展示 / 玩法预告 / 累充预告） | 丢 19 / 50，**不取正文** |
| ② 正文 | 解不出完整时段 | 再丢 11 / 31 |
| ③ 正文 | 含「活动系列 / 礼包 / 上架」的复刻帖 | 再丢 1 |

→ 一轮候选 ≈ **18 条**（约 3.6 条/周）；数据文件现 19 条（首轮 19 条手工条目已与管线取法对齐）。

两条与其它端不同的口径：**条目粒度 = 一篇帖子一条**，日期只认本篇自己的时段（不从兄弟帖
回填）；**标题只取「活动名」**，不取整条 `主题丨子标题`（`丨` 前缀是栏目名，`往期复刻` /
`轮替女神之影` / `商城上新` 都会撞）。又因为日期不会被校准自动改写，去重兜底走**精确口径**
（同名同起日或同源帖，`keys.EXACT_FALLBACK_GAMES`）；而卡池会返场、标题逐字相同，身份因此
**带开始日期**（`keys.DATED_BANNER_GAMES`）——后者还是前瞻性判据，窗口内尚未撞上。
复刻 / 返场帖的标题统一收成「活动名 + 限时复刻」（后缀接在引号外面）；它只把**首开与
复刻**两期分开，轮替池的各期标题仍逐字相同 —— 分期靠的还是开始日期。
「版本大活动」由正文的官方复合标签判（`大型〈季节〉活动` / `〈◯◯〉主题活动` / `〈◯◯〉联动活动`），
332 天全库只命中 4 个、零假阳性。

验证：离线自检 **124 项**（`themis/selftest.py` + `fixtures/` 的真实抓取快照）；dry-run 实盘
正文逐篇核对（1 篇风控 1034），首轮订正 6 条日期、归一 5 条标题。

### 6.7 自动采集目标源 (远期)

| 数据源 | URL | 内容 | 难度 |
|--------|-----|------|------|
| 米游社公告 | miyoushe.com | 各游戏官方公告 | ⭐⭐ |
| HoYoLAB | hoyolab.com | 国际服公告 | ⭐⭐ |
| B站官号 | space.bilibili.com/... | 版本PV、活动预告 | ⭐⭐⭐ (视频) |
| 游戏内公告 | API抓取 | 最准确的活动信息 | ⭐⭐⭐⭐ (需逆向) |

---

## 7. UI/UX 设计方向

### 7.1 色彩体系

遵循各游戏品牌色：

| 游戏 | 主色 | 色值 |
|------|------|------|
| 原神 | 金色/蓝 | `#FFCC32` / `#4A90D9` |
| 星穹铁道 | 星穹紫 | `#7B5EA7` |
| 绝区零 | 霓虹绿 | `#00E5A0` |
| 未定事件簿 | 玫瑰金 | `#D4929A` |
| 崩坏3 | 樱粉 | `#FF6B9D` |

### 7.2 布局草图

```
┌──────────────────────────────────────────────────┐
│  🎮 米游活动日历          🌙  [游戏筛选] [🔍]   │  ← 顶栏
├──────────────────────────────────────────────────┤
│         2026年 8月        ◀ ▶ 今天               │  ← 月份导航
├──────┬──────┬──────┬──────┬──────┬──────┬──────┤
│  一  │  二  │  三  │  四  │  五  │  六  │  日  │  ← 星期头
├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│      │      │      │      │      │  1   │  2   │
│      │      │      │      │      │      │      │
├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│  3   │  4   │  5   │  6   │  7   │  8   │  9   │
│      │      │ ━━━━ │      │      │ ════ │ ════ │  ← 活动色条
│      │      │ 原神 │      │      │ 铁道 │ 铁道 │
├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│  ...                                             │
├──────┴──────┴──────┴──────┴──────┴──────┴──────┤
│  📋 8月5日 活动详情:                             │  ← 详情面板（点击展开）
│  🟡 [原神] 「韶光抚月」 08/15 - 09/26           │
│  🟣 [铁道] 「星际之旅」 08/05 - 08/20           │
└──────────────────────────────────────────────────┘
```

### 7.3 交互原则

- **一眼看清**: 打开页面3秒内，用户应能判断"今天有没有活动"
- **最少点击**: 大部分信息在日历主视图直接可见
- **渐进式展示**: 日历格 → 活动色条 → 点击展开详情
- **移动端适配**: 月视图在手机上切换为列表模式

---

## 8. 项目里程碑与迭代计划

### 8.1 总体时间线

```
2026 Q3                     
├───────────────────────────┤
│  v1.0.0 全部 Sprint 完成   │
│  4周                       │
│  实际: 1天集中开发          │
```

### 8.2 详细迭代拆解

#### ✅ Sprint 0 — 项目初始化 (第1周) — 完成于 2026-07-29

| 任务 | 产出 | 状态 |
|------|------|------|
| 初始化 React + TS + Vite 项目 | 项目骨架 | ✅ |
| 配置 ESLint + Prettier | 代码规范 | ✅ |
| 设计数据模型 (Zod Schema) | 类型定义 + 元数据 | ✅ |
| 编写首批活动样本数据 (YAML) | 3款游戏 × 5条 = 15条样本 | ✅ |
| 初始化 CLAUDE.md 项目文档 | AI 开发指引 | ✅ |
| 初始化 Git 仓库 + develop 分支 | 版本控制 | ✅ |
| 创建 GitHub 仓库 / CI / 部署 | GitHub repo + Actions + Vercel | ✅ |

#### ✅ Sprint 1 — 日历核心组件 (第2-3周) — 完成于 2026-07-29

| 任务 | 产出 | 状态 |
|------|------|------|
| 月视图日历网格组件 | CalendarGrid + 三层架构 | ✅ |
| Paimon.moe 风格连续色条 | event-layout.ts 分段引擎 | ✅ |
| 活动个性化颜色 + 图片背景 | color / bar_bg_image 字段 | ✅ |
| 浅色色条 + 深色文字配色 | Tailwind 50/100 色阶 | ✅ |
| 色条点击 → 详情面板高亮 | onSelectEvent + highlightedEventId | ✅ |
| 月份导航 (上/下月, 回到今天) | Header 组件 | ✅ |
| 活动数据加载与解析 | DataLoader + Zod 校验 | ✅ |
| 今日高亮 + 活动日期标记 | today ring + 选中态 | ✅ |
| 暗色/亮色主题切换 | useTheme + CSS变量 | ✅ |
| YAML 数据文件接入 (替换硬编码) | Vite glob import + js-yaml | ✅ |
| 游戏筛选器 | GameFilter 横向按钮组 | ✅ |
| 活动类型筛选器 | TypeFilter 图标标签按钮组 | ✅ |
| 单元测试 | date-utils + event-layout / 26 tests | ✅ |

#### 🚀 Sprint 2 — 额外视图 & 上线部署

| 任务 | 产出 | 状态 |
|------|------|------|
| 活动详情面板 (点击展开) | EventDetail + 高亮联动 | ✅ 已完成 |
| 周视图 & 日视图 | 月/周/日三视图切换 | ✅ 已完成 |
| 创建 GitHub 仓库 | main + develop 分支已推送 | ✅ 已完成 |
| 今日高亮样式优化 | 蓝色实心圆 + 白色数字 | ✅ 已完成 |
| 活动完成状态 + 筛选 | localStorage + 卡片切换 + 筛选栏 | ✅ 已完成 |
| 筛选状态持久化 | usePersistedState + localStorage，刷新后保留筛选 | ✅ 已完成 |
| GitHub Actions CI/CD | push/PR 触发 → lint → test → build | ✅ 已完成 |
| 部署到 Vercel | https://hoyocalendar-nu.vercel.app | ✅ 已完成 |

#### 🔧 Sprint 3 — 数据工具 (独立模块)

| 任务 | 产出 | 说明 |
|------|------|------|
| 📥 公告解析器 | `tools/editor/extractor.py` 集成于 GUI | 米游社API获取 + 文本提取 → 填入表单 | ✅ 已完成 |
| ✏️ 可视化编辑器 | `tools/editor/` Python TUI | 终端交互式编辑，直接写入 YAML 文件 | ✅ 已完成 |
| 🔗 解析→编辑工作流 | 解析弹窗 → 填入表单 | ✅ 已完成 |
| 🏷️ 活动类型动态管理 | type_pool.json 持久化 + ✎ 管理弹窗 | ✅ 已完成 |
| #️⃣ 标签动态管理 | 跨游戏共享标签库 + 搜索 + chip展示 | ✅ 已完成 |
| 📅 日期选择器 | 📅 按钮弹出月历，年月导航 | ✅ 已完成 |
| 🔍 活动列表筛选排序 | 按日期/标题/类型排序 + 范围/搜索 | ✅ 已完成 |
| 🔗 活动链接字段 | 编辑器输入 + 前端可点击跳转 | ✅ 已完成 |
| 🎨 选色器 | 系统色盘 + RGB Spinbox + 预览 | ✅ 已完成 |
| 📦 一键启动 | editor.bat 双击打开编辑器 | ✅ 已完成 |
| 🚀 一键推送 | 编辑器内推送数据 → GitHub → Vercel 部署 | ✅ 已完成 |
| 🧪 解析器测试 | 多公告样本验证 | 覆盖不同游戏、不同活动类型的公告格式 |

---

## 9. Git 分支策略与协作规范

### 9.1 分支模型

```
main (部署分支，Vercel 自动部署，由 develop 合并而来)
└── develop (开发主分支)
    ├── feature/xxx               ← Web 端功能（React 前端）
    └── 编辑器工具 / 数据更新      ← 直接提交在 develop
```

### 9.2 分支命名规范

```
feature/<功能名>   # Web 新功能，如 feature/game-filter
bugfix/<描述>      # Web Bug 修复
refactor/<内容>    # 重构
docs/<内容>        # 文档更新
```

### 9.3 Commit 规范

采用 [Conventional Commits](https://www.conventionalcommits.org/)：

```
<type>(<scope>): <description>

feat(calendar): 实现月视图日历网格组件
feat(event): 添加活动详情弹窗
fix(calendar): 修复跨月活动显示错误
style(theme): 调整暗色主题配色
docs(readme): 更新项目说明文档
refactor(data): 重构事件数据加载逻辑
test(calendar): 添加日历组件单元测试
chore(deps): 升级 vite 到最新版本
```

### 9.4 实际开发流程（solo + AI 主导）

**Web 端功能：**

```
1. 从 develop 切出 feature 分支
2. 开发完成，npm run build 通过
3. 快进合并回 develop（git merge --ff-only）
4. 合并 develop → main（快进），触发 Vercel 自动部署
5. 删除已合并的 feature 分支
```

**编辑器 / 数据更新：**

```
1. 直接在 develop 提交（数据提交信息统一为 data: 更新活动数据）
2. 编辑器「一键推送」自动完成：提交 develop → push → merge main → push → 切回 develop
```

无强制 Code Review / PR / Squash Merge；核心规则是「禁止未经用户确认的 commit 和 push」。

---

## 10. GitHub 项目管理配置

### 10.1 Repository 结构

```
HoyoCalendar/
├── .github/
│   └── workflows/          # CI/CD 工作流 (ci.yml)
├── src/                    # 前端源代码 (React)
├── data/events/            # 活动 YAML 数据文件
├── tools/
│   └── editor/             # Python 编辑器 + 自动化维护管线
│       ├── gui.py          # tkinter 图形界面
│       ├── main.py         # 终端交互式界面
│       ├── maintain.py     # 无人值守入口：四端各跑「提取 → 落盘」
│       ├── common/         # 管线共用底层：extractor / bilibili / keys /
│       │                   #   calibrate / apply_events / colors / yaml_io / body_cache
│       ├── genshin/        # 原神管线（pipeline / inference / RULES.md / PLAN.md）
│       ├── starrail/       # 星铁管线（pipeline / parse / selftest / RULES.md / PLAN.md）
│       ├── zenless/        # 绝区零管线（pipeline / parse / inference / selftest / RULES.md / PLAN.md）
│       ├── themis/         # 未定管线（pipeline / parse / selftest / RULES.md / PLAN.md）
│       └── logs/           # 运行日志（按天追加）
├── editor.bat              # 一键启动编辑器
├── CLAUDE.md               # AI 开发指引
├── DEV.md                  # 开发者文档
├── DEVELOPMENT_PLAN.md     # ← 本文件
└── README.md
```

> 2026-09-21 更新：这个块此前还画着 `tools/editor/extractor.py`、`yaml_io.py`（模块搬迁进
> `common/` 之前的形态），属 §11.1 里登记的那条「过期结构块」，现已随文档梳理一并订正。

### 10.2 GitHub Project Board 列设置

```
📋 Backlog         →  🏗️ In Progress   →  👀 Review        →  ✅ Done
   │                      │                    │                  │
   │  Ideas &             │  Developer          │  PR Opened        │  Merged
   │  Future tasks         │  working on it      │  Under review     │  & Deployed
```

### 10.3 Issue Labels

| Label | 颜色 | 用途 |
|-------|------|------|
| `feature` | 🟢 `#0E8A16` | 新功能 |
| `bug` | 🔴 `#D73A4A` | 缺陷修复 |
| `docs` | 🔵 `#0075CA` | 文档 |
| `design` | 🟣 `#D4C5F9` | UI/UX设计 |
| `data` | 🟡 `#FBCA04` | 活动数据更新 |
| `tech-debt` | 🟤 `#8B4513` | 技术债 |
| `priority:high` | 🔴 `#FF0000` | 高优先级 |
| `sprint-1` | ⬜ `#E1E4E8` | Sprint 1 任务 |
| `game:genshin` | 🟠 `#FFCC32` | 原神相关 |
| `game:hsr` | 🟣 `#7B5EA7` | 铁道相关 |
| `game:zzz` | 🟢 `#00E5A0` | 绝区零相关 |

---

## 11. 风险与待确认项

### 11.1 风险清单

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| ~~模块搬迁后原神侧未做联网回归~~（2026-09-21 已解决） | ✅ 已解决 | 为分出 `common/` + `genshin/` + `starrail/` 而改了全部 import 与路径，原神侧「行为不变」曾**只有静态证据**（编译 + import 通过）。已跑 `python maintain.py` 双游戏联网回归：`data/events/genshin-impact.yaml` **md5 前后一致**（`8a6a2aad…`，50 条不变）。清单与实测见 `tools/editor/genshin/PLAN.md` §4 里「2026-09-21 — 模块搬迁进 `common/`」 |
| ~~文档里过期的仓库结构块~~（2026-09-21 已订正） | ✅ 已解决 | §10.1 的仓库结构此前还画着 `tools/editor/extractor.py`、`yaml_io.py`，没有 `common/`、`genshin/`、`starrail/`、`zenless/`（重构前就陈旧）。已随本轮文档梳理一并订正；`DEV.md` 里同一份结构块也一并更新，并加了三份 `PLAN.md` 的指路 |
| ~~B站活动时段解析与米游社侧口径不一致，写错过数据~~（2026-09-24 已修正） | ✅ 已解决 | 「结束时刻 `03:59` 才减一天、其余照抄」这条口径被写成了两份实现，B站侧 `common/bilibili.py` 的 `parse_activity_time` 漏掉时刻判断、一律减一天；而 `merge_activities` 让 **B站优先**，于是错的那份赢了——米游社正文其实已算对。实测落错 3 条：`「绮衣珍赏·晴暖」` 被记成 `2026-11-04 ~ 2026-11-03`（时段只有一个月日期，被同时当成起止，**起晚于止**）、`「巡历雪境，凝铸锋锐」`/`「桌上剧团·冒险的前夜会」` 的 end 各早一天。已修两侧同口径并**手工订正**这 3 条（`apply_events` 只新增、常规活动不在 `calibrate` 覆盖内，写错的行自己回不来）。清单与实测见 `tools/editor/genshin/PLAN.md` §4 里「2026-09-24 — B站日期口径修正」；日期闸目前只查「日期有没有」、不查「起 ≤ 止」，已记在 `genshin/PLAN.md` §2.3 供决策 |
| 官方公告格式频繁变动 | 🟡 中 | 多数据源冗余获取，出错时降级为手动。**已知未处理格式：三段式复合活动**（正文无 `〓活动时间〓`、改用带空格的 `〓 子活动名 〓` 分段，各段自带 `活动时间：`），实例「虹旅藏金·耀星烁熠」。这类条目被日期闸丢弃并在日志报出，需人工补录 |
| 抓取瞬时失败导致本轮漏条目 | 🟢 低 | 只新增 + 幂等，漏的条目下次成功运行会自动补上，不会写坏数据 |
| 米游社正文接口风控（`retcode 1034`） | 🟡 中 | 公告列表不受影响，正文大面积抓不到 → 被日期闸丢弃 → 本轮报「0 新增」，是**假阴性**（易被误当成「今天没有新活动」）。**治本：已落盘条目不抓正文**（`genshin/pipeline._needs_body` 用同一套去重预筛，请求数 20 → 稳态 0~2，根因是请求数）；**可观测：运行日志如实记录**抓取成败、风控计数、跳过正文数，供无人值守后人工核查。反复重跑会加重，勿硬刚，遇风控等冷却 |
| ~~绝区零存量数据待订正 10 条~~（2026-09-21 已订正） | ✅ 已解决 | 绝区零管线落地时用真实公告反查出既有手工数据的错误：6 条结束日漏减一天、2 条类型应为 `登录福利`、1 条高难结束日重叠、版本更新 3.1 的标题格式不统一。清单与依据见 `tools/editor/zenless/PLAN.md` §6.2。**这 10 条里没有一条能被校准自动修好**（`calibrate` 只覆盖 5 个类型、只处理 30 天窗口内、且类型本身不参与校准），所以全部手工落盘。id 保持不变（前端用 id 追踪「已完成」，改 id 会清掉标记；且去重主键不含 id，保留无副作用） |
| ~~绝区零总纲内、公告未发的活动会晚几天进日历~~（2026-09-21 已解决） | ✅ 已解决 | 「先按总纲落默认类型、等公告到了订正类型」所缺的那条订正通道 2026-09-21 补上了：`calibrate.correct_from_candidates` 按标题订正类型并补全 `pending` 条目。**已实盘跑通**：数据文件 45 → 48 条，新增的正是方案预测的那三条（含 3.1 那条已结束的「回归丽都 羽落重逢」）。方案与离线验证见 `zenless/PLAN.md` §1.2 |
| **`pending` 条目的类型/描述要等它自己的公告**（2026-09-21） | 🟢 低 | 从总纲落盘的条目带 `pending` 标记，缺 `description` 与配色。标记会让预筛（`keys.likely_recorded`）**必抓正文**，公告到了自动补齐并清标记。代价是该活动公告还在抓取窗口里时，每轮多一次请求；公告翻页刷出窗口后不再请求。若该活动的公告始终不存在（如「回归丽都 羽落重逢」实测没找到），则一直是默认类型 + 兜底配色，需人工订正 |
| **活动类主键含开始日期，日期被大幅订正会插重复**（2026-09-21 决策 8 的残余风险） | 🟢 低 | 日期成了身份的一部分，`find_duplicate` 用「同名且开始日期相差 ≤7 天」兜底。人工把某条的开始日期改动超过 7 天，下次运行会认不回而插一条重复。触发条件苛刻（要人工改日期且幅度 >7 天），且重复条目在数据文件里肉眼可见、删一条即可，不额外加机制 |
| 版本延期 / 高难排期调整 | 🟢 低 | 版本锚点从数据文件推导，人工改一次「版本更新」日期即可，后续推理自动跟着走；卡池等已打 `calibrated` 标记的条目由 B站权威来源改回 |
| 多游戏活动数据量膨胀 | 🟢 低 | YAML分文件管理，按版本归档历史 |
| 日历组件跨月活动渲染复杂 | 🟡 中 | 先在数据处理层拆分跨月事件 |
| 图片资源版权问题 | 🔴 高 | 优先使用官方公开素材 + 注明出处链接 |
| 移动端体验不佳 | 🟢 低 | MVP以桌面端为主，v0.2优化移动端 |

### 11.2 核心决策确认项 ✅

> **状态: 全部确认完毕 (2026-07-28)**

| # | 问题 | 决定 |
|---|------|------|
| 1 | 目标平台 | Web (React SPA) ✅ |
| 2 | UI框架 | React + Tailwind CSS ✅ |
| 3 | MVP数据管理 | 手动录入 YAML 文件，Git版本管理 ✅ |
| 4 | 后端 | MVP阶段不需要，纯前端静态站 ✅ |
| 5 | 首期游戏 | 原神 / 星穹铁道 / 绝区零 ✅ |
| 6 | 包管理器 | npm ✅ |
| 7 | 部署平台 | Vercel (免费) ✅ |
| 8 | 主题 | 暗色+亮色，默认跟随系统 ✅ |

---

## 附录

### A. 参考项目与灵感

- [HSR Tracker](https://github.com/kel-z/HSR-Tracker) — 星穹铁道活动追踪器
- [Paimon.moe](https://paimon.moe/) — 原神祈愿/活动追踪
- [Seelie.me](https://seelie.me/) — 原神角色养成规划
- [Genshin Wizard](https://genshinwizard.com/) — 原神活动日历

### B. 关键术语表

| 术语 | 说明 |
|------|------|
| 版本/Version | 米游约每6周一个大版本更新 |
| 卡池/Banner | 限时角色/武器抽取活动（通常21天） |
| 上半/下半 | 版本分为上下半场，各有不同卡池 |
| 长草期 | 版本末期活动较少的时间段 |
| 前瞻直播 | 版本更新前的官方直播预告 |

---

## 12. 补充确认项

以下是我认为还需要明确的决策点：

### 12.1 整体视觉风格

日历工具的设计气质会影响用户的第一印象和日常使用意愿。

| 方案 | 特点 | 参考效果 |
|------|------|----------|
| **🟢 简洁现代风** | 大量留白、清晰网格、弱化装饰元素。像 Google Calendar / Apple Calendar | 干净专业，信息层级清晰，开发成本最低 |
| **米游风格化** | 融入各游戏视觉元素：纹理背景、角色剪影、渐变品牌色、圆角玻璃效果 | 粉丝向强，但容易视觉疲劳，且图片资源版权敏感 |
| **混合方案** | 默认简洁风 + 可选的游戏主题皮肤（用户手动切换） | 兼顾两者，但工作量大，建议推迟到 v0.3 |

> **建议**: MVP阶段采用**简洁现代风**，用品牌色条做游戏区分，不引入复杂纹理。后续版本可加主题皮肤。

### 12.2 数据维护工作流

MVP阶段活动数据存储在 YAML 文件中，需要明确维护方式。

| 环节 | 建议做法 |
|------|----------|
| **谁来录入** | 开发者（你）直接编辑 `data/` 下的 YAML 文件 |
| **录入时机** | 各游戏版本前瞻直播后（约版本更新前1周） |
| **一次录入量** | 每个版本约5-15条活动 |
| **数据校验** | YAML 文件提交时，CI 自动运行 Zod schema 校验 |
| **维护工具** | 直接文本编辑；v0.3 后提供 Web 表单录入 |

### 12.3 开源许可证

代码放在 GitHub 公开仓库前需要选定许可证。

| 许可证 | 特点 |
|--------|------|
| **MIT** 🟢推荐 | 最宽松：别人可以随意使用/修改/商用，只需保留版权声明 |
| **GPL v3** | 传染性：别人修改后也必须开源 |
| **Apache 2.0** | 类似MIT但额外包含专利授权 |

> **建议**: MIT，对个人项目最友好，社区接受度最高。

### 12.4 浏览器兼容性

| 浏览器 | 支持策略 |
|--------|----------|
| Chrome / Edge (Chromium) | 🟢 主要支持（≥ 最新2个大版本） |
| Firefox | 🟢 主要支持（≥ 最新2个大版本） |
| Safari | 🟡 尽力兼容（macOS/iOS用户） |
| IE 11 | 🔴 不支持 |

### 12.5 测试策略

| 层级 | 覆盖范围 | 工具 | 目标 |
|------|----------|------|------|
| **单元测试** | 数据解析、工具函数、状态逻辑 | Vitest | 核心逻辑覆盖 |
| **组件测试** | 日历组件、筛选器、详情面板 | React Testing Library | 关键交互覆盖 |
| **E2E测试** | 核心用户流程 | Playwright (后期引入) | v0.2+引入 |
| **数据校验** | YAML 数据文件 schema | Zod + CI | 每次提交自动跑 |

MVP阶段不追求覆盖率数字，重点保障**数据解析不出错**和**日历渲染不崩溃**。

---

## 13. 开发规范与治理规则

### 13.1 CLAUDE.md 规范

项目根目录的 `CLAUDE.md` 是 AI 辅助开发的「说明书」。每次 AI 参与开发时，会自动读取该文件作为行为准则。应包含以下内容：

```markdown
# CLAUDE.md — HoyoCalendar 项目开发指引

## 项目概述
米游活动日历 — 展示原神/星穹铁道/绝区零活动日期的Web日历应用。

## 技术栈
- React 18 + TypeScript + Vite
- Tailwind CSS + shadcn/ui
- npm 包管理
- 数据: YAML 静态文件 + Zod 校验

## 开发规则
1. 组件放在 src/components/ 下，按功能分目录
2. 每个组件一个文件，同目录放对应的 .test.tsx 测试
3. 数据类型统一定义在 src/types/ 下
4. 新功能从 develop 分支创建 feature/<name> 分支
5. 提交前确保 npm lint 和 npm test 通过
6. 使用 Conventional Commits 规范

## 数据规则
1. 活动数据文件在 data/events/ 下，按游戏分文件
2. 修改数据文件后运行 npm validate-data 校验
3. 数据 schema 定义在 src/types/events.ts

## 设计规则
1. 游戏品牌色定义在 tailwind.config.ts 的 theme.colors 中
2. 使用 CSS 变量处理暗色/亮色切换
3. 优先使用 shadcn/ui 组件，不要重复造轮子
```

### 13.2 代码组织规范

```
src/
├── components/          # UI 组件
│   ├── calendar/        #   日历相关组件
│   │   ├── CalendarGrid.tsx
│   │   ├── CalendarGrid.test.tsx
│   │   ├── MonthNavigator.tsx
│   │   ├── EventBar.tsx
│   │   └── DayCell.tsx
│   ├── event/           #   活动相关组件
│   │   ├── EventDetail.tsx
│   │   └── EventCard.tsx
│   ├── filter/          #   筛选器组件
│   │   ├── GameFilter.tsx
│   │   └── TypeFilter.tsx
│   ├── layout/          #   布局组件
│   │   ├── Header.tsx
│   │   └── Sidebar.tsx
│   └── ui/              #   shadcn/ui 组件（自动生成）
├── types/               # TypeScript 类型定义
│   ├── events.ts        #   活动数据类型 + Zod schema
│   └── filters.ts       #   筛选器类型
├── hooks/               # 自定义 React Hooks
│   ├── useEvents.ts     #   活动数据加载
│   └── useTheme.ts      #   主题切换
├── lib/                 # 工具函数
│   ├── data-loader.ts   #   YAML 解析
│   └── date-utils.ts    #   日期计算
├── styles/              # 全局样式
│   └── globals.css      #   Tailwind + CSS变量
├── App.tsx
└── main.tsx
```

### 13.3 组件开发规范

1. **单一职责**: 每个组件只做一件事。日历网格负责排列格子，不负责加载数据。
2. **Props 优先**: 数据通过 props 传入，不在组件内部直接 import 数据文件。
3. **状态上提**: 多个组件共享的状态（如选中日期、筛选条件）提升到 `App.tsx` 用 Context 管理。
4. **无 magic number**: 日期格式化、颜色映射等常量统一在 `lib/constants.ts` 中定义。

### 13.4 数据变更流程

```
1. 创建分支: git checkout -b data/gi-4.0-events
2. 编辑数据: data/events/genshin-impact.yaml
3. 本地校验: npm validate-data
4. 提交: git commit -m "data(genshin): 添加 4.0 版本活动"
5. 发起 PR → develop
6. CI 自动校验数据格式 → 合并
7. Vercel 自动部署上线
```

### 13.5 Git 工作流实操

```
# 开始一个新功能
git checkout develop
git pull origin develop
git checkout -b feature/calendar-grid

# 开发中频繁提交
git commit -m "feat(calendar): 实现基础日历网格布局"
git commit -m "feat(calendar): 添加日期格子点击事件"

# 完成后推送并发起 PR
git push origin feature/calendar-grid
# → 在 GitHub 上创建 Pull Request → develop

# 合并后清理本地分支
git checkout develop
git pull origin develop
git branch -d feature/calendar-grid
```

### 13.6 GitHub 仓库初始化清单

在 Sprint 0 中需要完成的配置项：

- [x] 创建 GitHub 仓库 `HoyoCalendar`
- [ ] 设置分支保护规则（`main` 和 `develop` 禁止直接 push）
- [x] 启用 GitHub Actions
- [ ] 创建 Project Board（Kanban）
- [ ] 配置 Issue Labels（按 10.3 节）
- [ ] 创建 Issue Templates（功能请求 + Bug报告）
- [x] 配置 Vercel 关联 GitHub 仓库（自动部署）
- [x] 添加 `.gitignore`（node_modules, dist, .env 等）
- [x] 添加 LICENSE（GPL v3.0）
- [x] 编写 README.md（项目简介 + 本地运行说明）

---

## 14. 当前状态总结

| 维度 | 状态 |
|------|------|
| 核心决策 (11.2) | ✅ 全部 8 项已确认 |
| 补充确认 (12.x) | ✅ 全部采纳建议 |
| 开发规范 (13.x) | ✅ 已确认 |
| Sprint 0 — 项目初始化 | ✅ 完成 (2026-07-29) |
| Sprint 1 — 日历核心组件 | ✅ 完成 (2026-07-29, 13/13) |
| Sprint 2 — 额外视图 & 上线部署 | ✅ 完成 (2026-07-29, 7/7) |
| Sprint 3 — 数据工具 | ✅ 完成 (2026-07-29, 11/11) |
| 四条自动化维护管线 (6.3 / 6.4 / 6.5 / 6.6) | ✅ 全部落地并跑通（星铁 22 → 29 条、绝区零 45 → 48 条均已实盘；未定 2026-09-25 接入，**只增不改**） |
| 当前分支 | `feature/zenless-automation`（编辑器工具，可从 `develop` 提交） |
| 线上地址 | https://hoyocalendar-nu.vercel.app |
| 下一步 | ①实盘跑一轮验总纲优先（会往数据文件新增条目，需人工确认）②浏览器验收绝区零日历 ③星铁回补福利类/三倍掉落 |

---

